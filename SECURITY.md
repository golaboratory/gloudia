# セキュリティポリシー

## サポート対象バージョン

gloudia は `v0` 系のため、**セキュリティ修正は最新のマイナーバージョンにのみ提供**します。

| バージョン | サポート |
|---|---|
| v0.2.x | ✅ |
| v0.1.x 以前 | ❌ 使用しないでください（後述） |

> [!WARNING]
> **v0.1.0 は使用しないでください。**
> v0.1.0 は現在とはまったく異なる旧コードベースを指しており、認証を迂回可能な
> 既知の問題を含みます。`go get github.com/golaboratory/gloudia@v0.2.0` のように
> バージョンを明示して取得してください。

## 脆弱性の報告

**公開 Issue で脆弱性を報告しないでください。** 修正版を提供する前に攻撃者へ情報が渡ります。

以下のいずれかで非公開に報告してください。

1. **GitHub Security Advisory（推奨）**
   本リポジトリの [Security] タブ → [Report a vulnerability]
2. **メール**
   `akahane@golaboratory.jp`

### 報告に含めていただきたい情報

- 影響を受けるパッケージとバージョン（例: `middleware` v0.2.0）
- 脆弱性の種類（認証迂回、情報漏洩、インジェクション等）
- 再現手順、または PoC コード
- 想定される影響範囲
- 判明していれば修正案

### 対応の流れ

| フェーズ | 目安 |
|---|---|
| 受領の確認 | 3 営業日以内 |
| 影響評価と初期回答 | 7 営業日以内 |
| 修正版のリリース | 深刻度に応じて調整（Critical は可能な限り速やかに） |
| 公表 | 修正版リリース後、報告者と調整のうえ Security Advisory を公開 |

報告者のクレジット記載を希望される場合はその旨をお知らせください。

## 利用者側で必ず設定すべき項目

本ライブラリは安全側の既定値（fail-closed）を採用していますが、以下は**環境に応じた設定が必須**です。
未設定のまま本番運用すると、機能が動作しないか、想定した保護が得られません。

| 環境変数 | 未設定時の挙動 | 設定が必要なケース |
|---|---|---|
| `TRUSTED_PROXY_CIDRS` | `X-Forwarded-Host` / `X-Forwarded-For` / `X-Real-IP` を一切信頼しない | リバースプロキシ・ロードバランサの背後で運用する場合（**テナント解決とレート制限の前提**） |
| `WS_ALLOWED_ORIGINS` | WebSocket は同一オリジンからの接続のみ許可 | フロントエンドが API と別オリジンにある場合 |
| `CORS_ALLOWED_ORIGINS` | `http://localhost:5173`（開発用）のみ許可 | 本番ドメインから API を呼ぶ場合 |

詳細は [README.md の環境変数リファレンス](README.md#環境変数リファレンス) を参照してください。

## 鍵とシークレットの取り扱い

- `auth.NewTokenMaker` / `crypto.NewCryptor` に渡す鍵は、**必ず環境変数やシークレット管理基盤から供給**してください。ソースコードに埋め込まないでください。
- `auth.GenerateRandomKey()` は開発時の鍵生成用ヘルパーです。生成した鍵をコミットしないでください。
- 本リポジトリの CI では [gitleaks](https://github.com/gitleaks/gitleaks) による秘密情報スキャンを実行しています。
